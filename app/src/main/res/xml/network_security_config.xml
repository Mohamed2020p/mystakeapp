<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (http) is BLOCKED everywhere by default, except for the test
  domain below — this is what allows the https -> http fallback for the
  test page while keeping mystake.com strictly on https.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">chciken2website.rf.gd</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
